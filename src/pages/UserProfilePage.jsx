import PagePlaceholder from './PagePlaceholder';

const UserProfilePage = () => (
  <PagePlaceholder title="Profile" description="Phase 10 — edit profile stored in Firestore." />
);

export default UserProfilePage;
