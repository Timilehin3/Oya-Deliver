import PagePlaceholder from './PagePlaceholder';

const RegisterPage = () => (
  <PagePlaceholder title="Register" description="Phase 4 — create account with Firebase." />
);

export default RegisterPage;
